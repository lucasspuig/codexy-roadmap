# Supabase (mismo proyecto que el admin)
SUPABASE_URL=https://zvsqcbeupeyjtgdguwoc.supabase.co
SUPABASE_SERVICE_ROLE_KEY=<service role secreto>

# URL pública del admin — se usa para construir los links /r/<token> que retornan las tools
PUBLIC_APP_URL=https://plan.codexyoficial.com

# Token que Claude debe enviar en Authorization: Bearer <token>
# Generá con: openssl rand -base64 48
MCP_AUTH_TOKEN=CAMBIAR_por_algo_random_largo

# Puerto (default 3100)
PORT=3100
